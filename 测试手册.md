# 怪物狂潮（Monster Waves）— 测试手册

> 用于按模块记录测试内容：每个用例含【前置】【步骤】【预期】三栏，请在【结果】列勾选 ✅ 通过 / ❌ 失败（失败时注明现象，必要时导出错误报告 zip 到项目根目录）。
> 测试环境：`D:\game\mc\.minecraft\versions\测试MOD`（Forge 1.20.1-47.4.x），mods 含 tacz 1.1.8-hotfix、gunsmithlib、cloth-config、ftb-library。
> 版本记录：当前 v9.5.0（git 待提交）。

---

## 0. 测试准备

- [ ] 启动测试环境，确认游戏正常进入、无红字报错 正常
- [ ] 打开聊天栏，确认 `/monsterwaves` 有 Tab 补全（输入 `/monsterwaves ` 后按 Tab） 正常
- [ ] 确认配置文件存在：`config/monsterwaves.json` 正常
- [ ] 确认 P 键默认绑定「技能加点界面」（选项→控制→怪物狂潮 分类） 正常

---

## A. 技能点系统（核心）

### A1. 加点界面（P 键 / `skill gui`）
| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| A1-1 | 按 P 键 | 打开加点界面，顶部显示「技能点/已分配」 | | 正常
| A1-2 | 界面有搜索框、滚动、属性分组（原版 Minecraft / 永恒枪械工坊：零 等） | 分组标题中文，属性行显示中文名+当前值+已分配 | | 正常
| A1-3 | 点击「攻击力」行的 + 号 | 技能点 -1，当前值即时 +10%（10.0→11.0 级别），行内「已分配 +1」 | |正常
| A1-4 | 连续加点至技能点耗尽 | 剩余 0 后不可再加，出现红色提示 | |正常
| A1-5 | 把某属性加到 maxPoints（默认 50） | 显示「(已满)」不可加 | |正常
| A1-6 | 关闭并重新打开界面 | 已分配数据保留（服务端持久） | |正常

### A2. 重置
| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| A2-1 | 界面点「重置全部加点」 | 扣 5 技能点（resetCostPoints），全部分配清零并返还 | |正常
| A2-2 | 配置 `resetEnabled=false` + reload 后重开界面 | 重置按钮消失/禁用 | |正常
| A2-3 | 指令 `/monsterwaves skill reset <玩家> <属性>` | 返还该属性点数，不扣费 | |正常
| A2-4 | 指令 `/monsterwaves skill reset <玩家>` | 重置全部，不扣费 | |正常

### A3. 技能点获取模式（gainMode）
| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| A3-1 | 默认 LEVEL：升级 | 每升 1 级 +pointsPerLevel（默认 1）点 | |正常
| A3-2 | 改 `gainMode=XP` + reload，杀怪攒经验 | 每 xpPerPoint（默认 100）经验自动 +1 点 | |正常
| A3-3 | 改 `gainMode=DISABLED` + reload | 升级/经验不再得点 | |正常
| A3-4 | 指令 `/monsterwaves skill add <玩家> 10` | 玩家技能点 +10 | |正常
| A3-5 | 指令 `/monsterwaves skill set <玩家> 5` | 技能点精确设为 5 | |正常

### A4. 属性显示名与白名单（v9.4）
| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| A4-1 | 删除 `attributeDisplayNames` 里某条目 + reload | UI 仍显示该属性，名字回退为属性自身 lang（如「移动速度」），**不再显示原始注册名** | | 正常
| A4-2 | 删除 `attributeConfigs` 里某条目 + reload | 该属性从 UI 消失、不可加点；已分配点数自动返还 | | 正常
| A4-3 | 恢复默认后 reload | 一切恢复 | | 正常

---

## B. 枪械加成（gunsmithlib，v9.4 核心）

> 需要手持枪械（TaCZ 枪包）测试。属性：射速 rpm / 换弹速度 reload_speed / 子弹伤害 bullet_damage / 子弹速度 bullet_speed / 弹匣容量 ammo_capacity（全部百分比，每点 +10%）。

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| B-1 | 给「射速」加 5 点后开枪 | 实际射速明显变快（rpm 600→900 级别） | |正常
| B-2 | 给「换弹速度」加 5 点 | 换弹动作明显加快 | |正常
| B-3 | 给「子弹伤害」加 5 点 | 击杀怪物的伤害明显提高 | |正常
| B-4 | 给「弹匣容量」加 5 点 | 弹匣容量显示 30→45（按枪） | | 已移除：gunsmithlib 的 ammo_capacity 是**物品属性**（不挂玩家），v9.4.6 已从默认白名单移除 |
| B-5 | 不加点时与没装本 mod 手感一致 | 无加成 | | 去除后加成依旧在（v9.4.3 的 resetAll 残留 bug，**v9.4.4 已修，请重测**） |
| B-6 | 界面显示 | 枪械属性分组为「永恒枪械工坊：零」或 gunsmithlib 对应分组，中文名 | |正常

---

## C. 怪物波次与刷怪

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| C-1 | `/monsterwaves spawn minecraft:zombie 5` | 5 只僵尸在附近生成 | |正常
| C-2 | spawn 一个非 Mob 实体（如 `minecraft:item`） | 自动移除，不崩服 | |正常
| C-3 | `/monsterwaves difficulty` | 显示当前阶段与难度 | |正常
| C-4 | `/monsterwaves stage info / next / prev` | 阶段切换，显示阶段名/难度/剩余 | |正常
| C-5 | 配置某阶段 `mobEffects`（如力量效果）+ reload | 该阶段生成的怪自带效果 | | 保留但**默认空**（BUFF 交由其他 mod，如 Apotheosis） |
| C-6 | 配置某阶段 `attributeMultipliers`（生命/攻击/护甲）+ reload | 该阶段怪物属性按倍率变化 | |
| C-7 | 配置 `mobListOverride`（专属怪物池）+ reload | 该阶段只刷配置池里的怪 | |正常
| C-8 | 多次击杀怪物 | 每波间隔 spawnInterval tick，数量在 min/max 内，不超 maxMobsPerPlayer | |正常

---

## D. 统一掉落

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| D-1 | 配置 `normalLoot` 里一条 item+chance+minCount/maxCount + reload | 击杀怪物按 chance 掉落该物品，数量在区间内 | |正常
| D-2 | `lootGlobalChanceMultiplier` 调 0.5/2.0 | 掉落概率整体降/升 | |正常
| D-3 | `isPlayerAffected` 字段 | true=玩家击杀才掉，false=任意原因死亡都掉 | |正常
| D-4 | 难度提升后击杀 | `lootExtraCountPerLevel` 生效（额外数量随难度） | | 正常

---

## E. 大范围拾取

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| E-1 | 击杀怪物掉落物品，站在 6 格内 | 掉落物自动飞向玩家并被拾取 | | 正常
| E-2 | `pickupRange` 调大/调小 | 拾取范围变化 | | 正常
| E-3 | `pickupItems=false` | 只拾取经验不拾取物品 | | 正常
| E-4 | `pickupBlacklist` 加入某物品 | 该物品不被自动拾取 | | 正常
| E-5 | `pickupOnlyOwnDrops=true` | 只拾取自己击杀的掉落 | | 正常

---

## F. 经验加成

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| F-1 | 击杀怪物 | 经验 = 原经验 × experienceMultiplier（默认 1.0 不变） | | 正常
| F-2 | `experienceMultiplier=2.0` + reload | 经验翻倍 | | 正常
| F-3 | 高难度阶段击杀 | `experienceBonusPerDifficulty` 额外加成 | | 正常

---

## G. 安全空岛 / 休息维度

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| G-1 | `/monsterwaves safe` | 传送至休息维度（管理员无冷却） | | 正常
| G-2 | 空岛初始物品 | 加入时发放（giveOnJoin） | | 正常
| G-3 | 坠落至 fallTeleportY 以下 | 传送到 fallDestination 或重生点（fallToRespawnPoint） | | 正常
| G-4 | `/monsterwaves safe reset` | 重置并重建空岛 | | 正常
| G-5 | `islandRadius/islandBlock` 调整 + reload | 空岛大小/方块变化 | | 正常

---

## H. 刷怪维度与竞技场

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| H-1 | `/monsterwaves battle` | 传送至刷怪维度 | | 正常
| H-2 | `arenaEnabled=true` 时正常 | 竞技场机制可用（arenaSpawnY 生效） | | 正常
| H-3 | `enabledDimensions` 配置 | 只在列出的维度刷怪/生效 | | 正常

---

## I. 配置系统（Cloth Config）

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| I-1 | 打开配置界面（Catalogue → 怪物狂潮 → Config） | 顶部分类汉化（全局/生成设置/难度参数/阶段/技能点系统等） | | 正常
| I-2 | 修改任意配置并保存 | 写入 config/monsterwaves.json | | 正常
| I-3 | `/monsterwaves reload` | 不重启生效，提示已重新应用属性 | | 正常
| I-4 | 手改 json 格式错误 | 启动报错可读（JSON 语法错误提示）或 Cloth 回退默认，不崩服 | | 正常

---

## J. 指令集（全部）

| # | 指令 | 预期 | 结果 |
|---|------|------|------|
| J-1 | `/monsterwaves` | 列出全部子命令 | | 正常
| J-2 | `/monsterwaves stats` | 显示自己技能点与分配 | |正常
| J-3 | `/monsterwaves skill points` | 显示自己技能点（无需玩家参数） | |正常
| J-4 | `/monsterwaves skill points <玩家>` | 显示指定玩家 | |正常
| J-5 | `/monsterwaves skill reset <玩家> minecraft:generic.attack_damage` | 正常执行（**含冒号属性名不再报"参数后应有空格分隔"**） | | 正常
| J-6 | `/monsterwaves stage set <带空格的阶段id>` | 正常设置 | | 正常
| J-7 | `/monsterwaves player list` | 列出在线玩家 | | 正常
| J-8 | `/monsterwaves reload` | 配置热重载 | | 正常
| J-9 | 所有带 `<玩家>` 参数指令 | Tab 补全在线玩家 | | 正常

---

## K. 稳定性 / 回归

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| K-1 | 连续加点/重置/重开界面 20 次 | 不卡、数值不错乱 | |
| K-2 | 快速换维度（safe↔battle） | 技能点数据保留、属性重新应用 | | 正常
| K-3 | 断开重进 | 技能点与分配持久（NBT） | | 正常
| K-4 | 击杀大量怪物（30+ 同时） | 无明显卡顿/崩溃 | | 会卡顿，属于游戏问题，无视
| K-5 | 关闭 mod 重开（升级 jar） | 旧数据兼容（v9.0 旧属性球数据迁移逻辑不报错） | | 正常
| K-6 | 检查日志 | 无异常堆栈（latest.log 中无 MonsterWaves 错误） | | 正常

---

## 测试记录（每轮）

| 日期 | 版本(git) | 模块 | 结果摘要 | 遗留问题 |
|------|-----------|------|----------|----------|
| | | | | |
| | | | | |
| | | | | |

---

## 已知边界（非 bug）

- 配置界面 POJO 子字段（如掉落条目 item/minCount）显示英文字段名——Cloth Config 11.1.136 限制，含义见《开发手册.md》第 3 节
- 未安装 gunsmithlib 的环境：枪械属性行不显示（白名单键解析不到），其余功能不受影响
- `minecraft:generic.block_break_speed` 原版 1.20.1 无此属性，需模组提供同名属性才生效
- 指令 `skill reset` 的属性参数**无需数量参数**（末尾多余内容会被当作属性名一部分并提示不存在）
