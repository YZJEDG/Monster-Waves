# 怪物狂潮（Monster Waves）— 开发手册

> 调试向手册，与《mod概述.md》（规格 v8.3）配套。**每次完成模块后更新本文件**。
> 当前版本对应：**MVP 阶段完成**（生成引擎 / 属性球 / 玩家属性 / 阶段 / 基础指令），Forge 47.4.10。

---

## 1. 构建与运行

| 项目 | 内容 |
|------|------|
| 构建命令 | `gradlew.bat build`（项目根目录，Windows） |
| 产物 | `build/libs/monsterwaves-0.1.0.jar` |
| 安装位置 | `D:\game\mc\.minecraft\versions\TAC_Z 神化\mods`（主整合包）/ `D:\game\mc\.minecraft\versions\测试MOD\mods`（测试环境） |
| 部署命令 | 项目根目录 `bash deploy.sh`：自动备份旧版到 `测试MOD\mods_backup\*.previous.jar`，再复制最新 jar（只复制 mod 本体） |
| JDK | 本地 `.jdk\jdk-17.0.19+10`（gradlew.bat 自动探测，无需配 JAVA_HOME） |
| Gradle | wrapper 8.4，用户缓存 `.gradle_home`（gradlew.bat 自动探测 GRADLE_USER_HOME） |
| 镜像 | 腾讯（gradle 分发）/ 阿里云（maven 依赖）/ maven.minecraftforge.net / parchmentmc |
| Forge | `1.20.1-47.4.10`，mappings `parchment 2023.09.03-1.20.1` |

**构建排错**：控制台 GBK 乱码时用 `iconv -f GBK -t UTF-8 日志文件` 转码；首次构建慢（下载依赖）。

## 2. 源码结构（src/main/java/com/mcmod/monsterwaves/）

| 类 | 职责 |
|----|------|
| `MonsterWavesMod` | @Mod 主类。**必须无参构造器**（1.20.1 限制）；用 `FMLJavaModLoadingContext.get().getModEventBus()` 注册物品（47.4.10 已 deprecated，勿改构造器注入——那是 1.20.4+ 机制） |
| `config/MWConfig` | Cloth Config 配置类（AutoConfig，@Config 注解，JSON 序列化（GsonConfigSerializer，config/monsterwaves.json））；存 `config/monsterwaves.json`，游戏内 Mods 列表 -> Config 按钮开 GUI；服务端通过 `MWConfig.get()` 读取 |
| `attribute/ModAttributes` | ~~v9.0 占位属性注册~~（v9.4 已删除）——tacz namespace 占位属性 TaCZ 不读取，枪械加成改由 gunsmithlib（Gunsmith Library）提供 |
| `data/PlayerDataManager` | **技能点（v9.0）**：NBT 存储（技能点/已分配/XP缓冲）、旧属性球数据一次性迁移、AttributeModifier 应用（百分比属性乘法）、事件发布 |
| `api/SkillPointGainEvent` 等 3 个 | 技能点 API 事件（获取/消耗/重置，可取消，供其他 mod 自定义算法） |
| `network/NetworkHandler` 等 5 类 | SimpleChannel：C2S 请求同步/加点/重置，S2C 数据同步/打开界面 |
| `client/SkillScreen` 等 4 类 | **FTB Library 加点界面**（`ButtonListBaseScreen`：搜索框+滚动+标题栏，列表首行重置，属性行点击加点）、客户端数据缓存、按键 |
| `build.gradle` 依赖 | **ftb-library-forge 1902.4.2-build.700**（maven.ftb.dev/releases，1902=1.20.1；文档所写 1902.1.11-build.46 不存在、maven 路径为 releases 复数）；mods.toml 前置 ftblibrary side=CLIENT |
| `stage/StageManager` | 阶段列表 + 切换广播 |
| `stage/StageData` | 阶段状态 SavedData（持久化到主世界 level.dat，键 `monsterwaves_stage`） |
| `spawn/MobSpawnManager` | 生成引擎（tick 调度、位置选择、生物池、难度应用） |
| `item/ModItems` | 物品注册（DeferredRegister，返回符咒/战斗符咒） |
| `item/ReturnCharmItem` | 返回符咒（右键传送休息维度，带冷却，成功后消耗） |
| `item/BattleCharmItem` | 战斗符咒（右键传送刷怪维度 arena，带冷却，成功后消耗） |
| `safe/SafeDimensionManager` | 休息维度：空岛生成（含功能方块）、传送与冷却、玩家规则、跳下传送；`IslandData` 空岛幂等标记 |
| `arena/ArenaDimensionManager` | 刷怪维度：超平坦战场，传送入口、出生点、启用开关 |
| `event/ModEventHandler` | 服务端事件：tick（阶段+生成+拾取+休息维度规则）、登录/重生/换维度技能点属性应用、击杀统一掉落、经验加成、技能点获取（升级/经验模式）、指令注册 |
| `command/MonsterWavesCommand` | /monsterwaves 指令树 |

资源：`src/main/resources/`：`pack.mcmeta`（**必须存在**，缺失则 Forge 不索引 assets）、`META-INF/mods.toml`、`assets/monsterwaves/{lang,models,textures}`。

## 3. 配置（Cloth Config，config/monsterwaves.json）

> 游戏内入口：Mods 列表 → 怪物狂潮 Monster Waves → Config 按钮。修改保存后即时生效（服务端每次读取最新值）。
> 前置依赖：cloth_config >= 11.1.118（规格 v8.3 的 ConfigType.SERVER 写法在 11.1.136 不存在，采用标准 AutoConfig 单文件配置）。

| 字段 | 分类 | 默认值 | 说明 |
|------|------|-----|------|
| `enabled` | general | true | 全局开关 |
| `spawnInterval` | spawn | 40 | 生成检测间隔（tick） |
| `minDistance` / `maxDistance` | spawn | 20 / 32 | 生成距玩家距离范围 |
| `maxMobsPerPlayer` | spawn | 30 | 玩家 48 格内本mod怪物上限 |
| `mobCountMin` / `mobCountMax` | spawn | 1 / 2 | 每波数量范围 |
| `mobStatRadius` | spawn | 48.0 | 数量统计半径 |
| `mobPool` | spawn | zombie:5, skeleton:3, creeper:2 | 怪物池（每项 `注册名:权重` 字符串） |
| `healthBonusPerLevel` | difficulty | 0.2 | 每点难度生命增幅（乘） |
| `attackBonusPerLevel` | difficulty | 0.5 | 每点难度攻击增幅（加） |
| `armorBonusPerLevel` | difficulty | 0.5 | 每点难度护甲增幅（加） |
| `skillEnabled` | skillSystem | true | 技能点系统总开关 |
| `pointsPerLevel` | skillSystem | 1 | 每升一级获得的技能点（LEVEL 模式） |
| `gainMode` | skillSystem | LEVEL | 获取模式：LEVEL / XP / DISABLED（开发者可监听 SkillPointGainEvent 自定义） |
| `xpPerPoint` | skillSystem | 100 | XP 模式每技能点所需经验 |
| `maxTotalPoints` | skillSystem | -1 | 总分配上限（-1=无限） |
| `attributeConfigs` | skillSystem | 10 项（全部百分比） | **加点白名单**：属性注册名→配置（enabled/percentage/maxPoints/percentagePerPoint）；未列出的属性不可加点（界面不显示）；默认 10 项=攻击/护甲/生命/速度/攻击速度/挖掘速度/**幸运/盔甲韧性**/tacz射速/tacz换弹，全部百分比；每属性可单独改回数值/幅度/上限 |
| `keyBinding` | skillSystem | key.keyboard.p | 加点界面按键 |
| `attributeDisplayNames` | skillSystem | 7 个原版 | 属性注册名→显示名 |
| `attributeConfigs.enabled/percentage/maxPoints/percentagePerPoint` | skillSystem | — | 每属性独立配置：是否可加 / 是否百分比 / 上限（-1=无限）/ **每点百分比幅度**（0=跟随全局 percentagePerPoint） |
| `percentagePerPoint` | skillSystem | 0.1 | 百分比属性每点加成比例（0.1=+10%） |
| `resetCostPoints` | skillSystem | 5 | GUI 重置消耗（从返还中扣除） |
| `resetEnabled` | skillSystem | true | 允许重置 |
| `experienceEnabled` | drop.experience | true | 经验加成开关 |
| `experienceMultiplier` | drop.experience | 1.0 | 经验倍率 |
| `experienceBonusPerDifficulty` | drop.experience | 0.2 | 每点难度经验加成 |
| `lootEnabled` 等 4 项 | loot | 见下 | 统一掉落：开关/全局倍率/每难度额外数量/normalLoot 条目 |
| `battleCharmEnabled` 等 3 项 | battle | true/true/600 | 战斗符咒：开关/开局给予/冷却 |
| `pickupEnable` 等 7 项 | pickup | 见下 | 大范围拾取：开关/半径6/拾取物品/**拾取经验球**/间隔5/仅自己的掉落/黑名单 |
| `stages`（List） | stage | 3 个默认阶段 | 阶段列表（各阶段 id/difficulty/duration 分别可调） |
| `enabledDimensions` | dimensions | 空（全部启用） | 本模组刷怪维度白名单；休息维度始终不刷怪 |
| `safeEnabled` | safe | true | 休息维度/返回符咒开关 |
| `islandRadius` | safe | 20 | 空岛半径 |
| `islandBlock` | safe | grass_block | 空岛顶部方块 |
| `giveOnJoin` | safe | true | 开局给予返回符咒 |
| `safeCooldown` | safe | 600 | 符咒冷却（tick） |
| `fallTeleportY` | safe | -10 | 跳下传送触发 Y |
| `safeSpawnY` | safe | 65 | 空岛出生 Y |
| `fallDestinationDimension` | safe | minecraft:overworld | 坠落目标维度（arena 建后改回） |
| `fallDestinationX/Y/Z` | safe | 0/64/0 | 坠落目标坐标 |
| `useCustomFallDestination` | safe | 已移除 | 由 `fallMode`/`fallToRespawnPoint` 取代 |
| `arenaEnabled` | arena | true | 刷怪维度开关 |
| `arenaSpawnY` | arena | 4 | 刷怪维度出生点 Y |

### 掉落条目字段（normalLoot 条目，GUI 显示英文字段名，含义如下）

| 字段（GUI 显示） | 中文含义 | 说明 |
|------|------|------|
| `item` | 物品 | 物品注册名，如 `minecraft:diamond`（支持任意模组物品） |
| `nbt` | NBT | 附加 NBT 字符串（可空），如 `{CustomTag:1}` |
| `minCount` | 最小数量 | 掉落数量下限（≥1） |
| `maxCount` | 最大数量 | 掉落数量上限（≥minCount） |
| `chance` | 概率 | 基础概率 0~1；最终概率 = chance × 难度 × 全局倍率（封顶 1） |
| `isPlayerAffected` | 受抢夺影响 | 是否受抢夺（Looting）附魔加成 |

### 技能点系统配置说明（skillSystem）

| 概念 | 说明 |
|------|------|
| 获取 | **默认随玩家等级**：每升 1 级 +pointsPerLevel 技能点（`gainMode=LEVEL`）；也可切 XP 模式（每积累 xpPerPoint 经验 +1）或 DISABLED；开发者可监听 `SkillPointGainEvent` 实现自定义获取算法 |
| 属性来源 | **动态读取玩家全部可同步属性**（`AttributeMap.getSyncableAttributes()`），但只显示/可加 **`attributeConfigs` 白名单**内的属性；**白名单外属性自动清理**（移出白名单后：移除 modifier、清除分配并返还技能点，见 applyAll） |
| 界面排序 | **固定顺序**：按属性注册名 namespace 分组（**自动读取属性来源 mod**，minecraft 置顶、其余按命名空间字母序），组内按注册名排序——不随已分配点数动态变化 |
| 加点 | 每点 +1 基础值（ADDITION）；该属性配置 `percentage=true` 时每点 +percentagePerPoint（MULTIPLY_TOTAL，适合移动速度等低基础值属性） |
| 白名单/上限 | **每属性独立配置**（`attributeConfigs`：enabled/percentage/maxPoints），默认仅 攻击/护甲/生命/速度/挖掘速度/攻击速度；总分配上限 `maxTotalPoints` |
| 重置 | GUI 重置全部：返还已分配点数 - `resetCostPoints`；指令重置免费；单属性重置免费返还 |
| 存储 | 玩家 NBT `monsterwaves_data`：`skillPoints` / `allocated`（属性注册名→点数）/ `xpBuffer` / `skillMigrated` |
| 迁移 | 旧属性球数据 `attributes` 段一次性迁移：每属性值/2 → 已分配点数（向下取整），另补偿总值 20% 技能点；完成后打 `skillMigrated` 标记 |
| 事件 | `SkillPointGainEvent`（获取，可改数量/取消）、`SkillPointSpentEvent`（加点消耗）、`SkillPointResetEvent`（重置）——均发布到 Forge 总线 |
| GUI | FTB Library `ButtonListBaseScreen`：搜索框+滚动+标题栏（等级/技能点/已分配）；**按 mod 分组**（组标题**可点击展开/收起**，折叠显示组内数量）；首行重置按钮，属性行点击加点；白名单仅能改 **JSON 文件**（Cloth GUI 对 Map 不支持编辑） |
| 指令 | `/monsterwaves skill points/add/set/reset/gui`、`stats`（查看技能点与分配） |

## 4. 阶段（可配置，各阶段难度/时长分别调整）

### 阶段字段（stages 列表条目，GUI 显示英文字段名，含义如下）

| 字段（GUI 显示） | 中文含义 | 说明 |
|------|------|------|
| `id` | 名称 | 阶段名称（纯文字，用于指令 `stage set <id>` 与切换广播） |
| `difficulty` | 难度系数 | **整体难度参数**：参与怪物生命/攻击/护甲、统一掉落概率与数量、经验加成等全部运算 |
| `duration` | 时长 | 持续 tick；-1 为无限（不自动推进） |
| `mobListOverride` | 专属怪物池 | 该阶段使用的怪物池（"注册名:权重"，空=用全局 mobPool）；如终局只刷强力怪 |
| `attributeMultipliers` | 属性倍率 | 子对象：`healthMultiplier` 生命倍率 / `attackMultiplier` 攻击倍率 / `armorMultiplier` 护甲倍率（1.0=无加成） |
| `mobEffects` | 自带BUFF | 该阶段怪物生成的药水效果列表（子字段：`effect` 效果注册名 / `amplifier` 等级 / `duration` 时长(-1无限) / `chance` 概率 / `showParticles` / `showIcon`） |

> 阶段列表存于 Cloth Config（`stage.stages`），可在 GUI/配置中分别调整各阶段难度与时长；`StageManager.getStages()` 动态读取。

> 休息维度系统（monsterwaves:safe）：
> - 维度由数据包注册（`data/monsterwaves/dimension` + `dimension_type`，flat 全空），空岛由代码生成（幂等，`IslandData` 标记）
> - 空岛设施：中心十字 工作台/熔炉/箱子/高炉/烟熏炉/切石机 + 附魔台（5x5 外环 **16 书架**满级）
> - 玩家规则：不刷怪（`MobSpawnEvent.PositionCheck` DENY + 生成引擎跳过）、不饥饿（锁 20/10）、免疫伤害（`LivingHurtEvent` 取消）、跳下传送（Y<-10 → 默认**刷怪维度 arena**）
> - 返回符咒 `monsterwaves:return_charm`：开局给予 / 合成（4 末影珍珠+1 金苹果）/ 右键传送，冷却 600 tick 存玩家 NBT
> - 维度开关：`enabledDimensions` 白名单（空=全部）；1.20.1 用 `MobSpawnEvent`（无 `LivingSpawnEvent`）
>
> 刷怪维度系统（monsterwaves:arena）：
> - 数据包注册，超平坦 grass/dirt/stone 三层（y=0 石 / y=1 土 / y=2 草），出生点 (0,4,0)
> - 专用于战斗：原版自然生成被拦截（`MobSpawnEvent.PositionCheck` DENY），本模组生成引擎接管
> - 传送入口：`/monsterwaves battle`（管理员）；休息维度坠落目标默认 arena
> - 配置：`arenaEnabled`、`arenaSpawnY`（arena 分类）

| 索引 | id（纯文字，无符号） | difficulty | duration(tick) |
|------|----|-----------|----------------|
| 0 | 萌芽期 | 1.0 | 6000 |
| 1 | 激战期 | 2.5 | 12000 |
| 2 | 终局之战 | 5.0 | -1（无限） |

- 自动推进：计时到 duration 切换，广播全体玩家；手动：`/monsterwaves stage next|prev|set <id>`
- 持久化：SavedData `monsterwaves_stage`（stageIndex + stageTimer），跨重启保留

## 5. 指令（/monsterwaves，全部支持 Tab 补全）

| 指令 | 权限 | 说明 |
|------|------|------|
| `spawn <生物id> [数量]` | op | 生成带标记+难度应用的怪；非 Mob 实体自动移除（防强转崩溃）。`<生物id>` 补全所有已注册非 MISC 实体（如 `minecraft:zombie`） |
| `stats [玩家]` | 全员 | 查看技能点与已分配属性。`[玩家]` 补全在线玩家 |
| `difficulty` | 全员 | 当前阶段+难度 |
| `safe` | op | 传送至休息维度（管理员，无冷却） |
| `safe reset` | op | 重置并重建空岛（景观刷新） |
| `battle` | op | 传送至刷怪维度（管理员） |
| `leave` | 全员 | **返回主世界**（优先玩家重生点/床，否则主世界出生点）——从安全/战斗维度返回的常规手段 |
| `stage info / next / prev / set <id>` | info 全员，其余 op | 阶段管理。`set <id>` 补全阶段 id（**greedyString**：id 含空格如"🌱 萌芽期"也能输入） |
| `skill` | 全员 | 查看自己技能点（等价 stats） |
| `skill points <玩家>` | 全员 | 查看指定玩家技能点与分配 |
| `skill add <玩家> <数量>` | op | 发放技能点（1~99999） |
| `skill set <玩家> <数量>` | op | 设置技能点（0~99999） |
| `skill reset <玩家> [属性]` | op | 重置全部（免费返还）或单属性（返还该属性点数）；`[属性]` 为属性注册名 |
| `skill gui` | 全员 | 打开加点界面（S2C 包） |
| `reload` | op | 重新加载 JSON 配置并重新应用全部在线玩家属性（改完配置文件无需重启；白名单外分配自动清理返还） |
| `player list` | 全员 | 列出所有在线玩家 |

## 6. 数据与标记

- 玩家 NBT：`monsterwaves_data` → `skillPoints`（可用技能点）/ `allocated`（属性注册名→点数）/ `xpBuffer`（XP 模式缓冲）/ `skillMigrated`（迁移标记）
- 旧属性球数据（`attributes` 段）首次访问一次性迁移：值/2 → 已分配点数 + 总值×20% 补偿技能点，随后删除
- 属性应用：技能点 modifier UUID = `nameUUIDFromBytes("monsterwaves:" + 属性注册名)`（稳定）；加法属性 ADDITION +每点1，百分比属性 MULTIPLY_TOTAL +每点10%
- 怪物标记：`monsterwaves_spawned=true`（生成引擎打标，用于数量统计）
- 掉落物归属：`monsterwaves_owner`（击杀者 UUID，供 onlyOwnDrops 使用）

## 7. 技能点系统（v9.0，替代属性球）

- **获取（默认随等级）**：`PlayerXpEvent.LevelChange` 正向升级 → grantPoints(等级数 × pointsPerLevel)；`gainMode=XP` 时 `XpChange` 累积到 `xpBuffer`，每满 xpPerPoint 发 1 点；`DISABLED` 不自动获取
- **经验加成（drop.experience）**：`LivingExperienceDropEvent`，敌对怪物经验 = 原值 × (multiplier + bonusPerDifficulty × (难度-1))，加速升级得点
- **加点**：`SkillPointSpentEvent` 可取消；校验 开关/属性存在/attributeEnabled/perAttributeMaxPoints/maxTotalPoints/可用点数，成功则点-1、allocated+1、重应用 modifier
- **重置**：GUI 全部重置返还 -resetCostPoints（resetEnabled=false 时禁用）；指令重置免费；单属性免费返还
- **GUI**：`SkillScreen`（P 键/`skill gui` 打开）——动态枚举 `AttributeMap.getSyncableAttributes()`（原版+模组属性），每行 显示名（配置 attributeDisplayNames > 原版属性名）+ 当前值 + 已分配 + [+]，滚轮滚动；数据经 SimpleChannel 同步（打开请求→C2SRequestSync，操作后 S2C 回发刷新）
- **API 事件**（`com.mcmod.monsterwaves.api`）：SkillPointGainEvent（可改数量/取消）、SkillPointSpentEvent、SkillPointResetEvent——供其他 mod 实现自定义获取算法/联动

> 注意：1.20.1 的 `AttributeMap` 无 `getAttributes()/getAllAttributes()`（1.20.2+），枚举用 `getSyncableAttributes()`；`Button` 用 `Button.builder()...build()`（6 参构造器是 1.20.5+）。

## 8. 已知限制 / 调试要点

| 项 | 说明 |
|----|------|
| 生成维度 | MVP 在所有维度生成（未做维度覆盖/刷怪维度，第二阶段实现） |
| 精英/Boss | 未实现（MVP 仅普通怪） |
| 配置 | **已迁移 Cloth Config**（config/monsterwaves.json + GUI）；阶段列表（STAGES）仍硬编码，后续"阶段配置化"处理 |
| 控制台乱码 | gradle 输出 GBK，用 iconv 转码 |
| 模型/资源 | **pack.mcmeta 必须随 jar 打包**（缺失 → Forge 不索引 assets → 模型/语言全失效） |
| @Mod 构造器 | 1.20.1 **只支持无参构造器**（改 IEventBus 注入会 NoSuchMethodException 崩溃） |
| FMLJavaModLoadingContext | 47.4.10 deprecated，但 1.20.1 无替代，用 @SuppressWarnings 压制 |
| 游戏内验证 | 进主世界看刷怪 → 击杀看掉落 → 靠近看吸附/提示；`/monsterwaves difficulty|stage info|stats` 查状态 |

## 9. 后续阶段规划（用户 2026-08-03 调整后）

**优先级原则：Cloth Config 等基础配置能力前置；对接其他 mod API 的项目最后做。**

1. **第二阶段（前置）**：Cloth Config 配置化——把 MWConfig 硬编码全部迁移为可配置（生成/阶段/属性球/掉落/维度），mods.toml 声明 cloth_config 前置依赖（>=11.1.118，整合包已有 cloth-config-11.1.136）；运行时可改
2. **第二阶段（后续）**：休息维度+返回符咒、刷怪维度、统一掉落系统、完整指令集、属性球清理等本 mod 自身功能
3. **第三阶段**：怪物 BUFF、精英/Boss、大范围拾取、HUD 等
4. **最后**：对接其他 mod API——FTB 团队（如共享掉落）、TaCZ 附魔兼容、JEI/EMI 集成等

## 10. 变更记录

| 日期 | 提交 | 内容 |
|------|------|------|
| 2026-08-03 | 49e1189 | MVP 基线：核心循环全套 |
| 2026-08-03 | c2d76ac | 审查修复 7 项（强转/位置偏移/持久化/重复拾取等） |
| 2026-08-03 | d464de0 | 属性球经验球式（悬浮+吸附+吸收） |
| 2026-08-03 | a6d268c | 补 pack.mcmeta；Forge→47.4.10；主类构造器注入（**随后撤回**） |
| 2026-08-03 | aa949eb | 恢复无参构造器（修复 NoSuchMethodException 启动崩溃） |
| 2026-08-03 | （本次） | 指令 Tab 补全（spawn 实体 / stage set id / ball 类型）；stage set 改 greedyString 支持含空格阶段 id |
| 2026-08-03 | （本次） | 新增独立《指令手册.md》（格式 `<指令>:<作用> 参数解析`） |
| 2026-08-03 | （本次） | 指令新增 set 功能：`ball set`（覆盖设置属性值）、`player` 组（add/set/reset/list，对齐规格 15.7） |
| 2026-08-03 | （本次） | 修复属性球往天上飞：按经验球机制重写移动（重力 0.04 + 空气阻力 + 衰减加速 + 出生延迟 + 目标胸口高度 + hasImpulse 同步） |
| 2026-08-03 | （本次） | 掉落机制事件化：新增 `AttributeBallDropEvent`（怪物死亡即触发，可取消/改概率/类型/数量），掉落流程改为事件驱动 |
| 2026-08-03 | （本次） | 非本mod生成的怪物也可掉落属性球：新增开关 `DROP_BALLS_FROM_ALL_MOBS`（默认 true），适用范围限定敌对 Monster |
| 2026-08-03 | （本次） | 新增 `deploy.sh` 部署脚本：备份旧版到 `测试MOD\mods_backup` 后复制新 jar（只复制 mod 本体）；交付约定纳入每轮流程 |
| 2026-08-03 | （本次） | 修复属性球漂着不被吸收：吸收判定改 AABB 相交（原中心距离判定使球悬在胸口高度追着玩家漂） |
| 2026-08-03 | （本次） | 属性球强制落地：废弃经验球式物理（setNoGravity+重力模拟曾被投诉飞上天），恢复原生重力+水平拉取为主，垂直限幅 0.2 |
| 2026-08-03 | （本次） | 修复 `spawn` 指令解析：`word()` 不允许冒号 → 改用 `ResourceLocationArgument.id()` + `getId()`（1.20.1 方法名，非 getResourceLocation/EntitySummonArgument） |
| 2026-08-03 | （本次） | 阶段名改纯文字：`🌱 萌芽期`→`萌芽期`、`⚔️ 激战期`→`激战期`、`🔥 终局之战`→`终局之战`（去表情/空格，便于输入编辑） |
| 2026-08-03 | （本次） | 团队系统：软依赖 FTB Teams（反射调用，无硬依赖）；新增 `team/TeamManager` + `team` 指令；未安装/异常时静默禁用 |
| 2026-08-03 | （本次） | 移除团队功能（按用户要求）：本 mod 不含任何团队功能，团队完全交由 FTB Teams 负责；删除 TeamManager/TeamInfo/team 指令 |
| 2026-08-03 | （本次） | **Cloth Config 配置化**：MWConfig 迁移为 @Config 类（config/monsterwaves.json + GUI）；mods.toml 声明 cloth_config 前置；全部引用点改运行时读取；注：规格的 ConfigType.SERVER 在 11.1.136 不存在，用标准 AutoConfig |
| 2026-08-03 | （本次） | 修复 Config 按钮不显示（Catalogue 模组目录替换原版 Mods 列表导致 Cloth 自动按钮失效）：注册 `ConfigScreenHandler.ConfigScreenFactory.class` 扩展点（1.20.1 用 IExtensionPoint，非旧版 ExtensionPoint 枚举） |
| 2026-08-03 | （本次） | 配置界面中文化：补充 Cloth Config 全部翻译键（title/category/option/@Tooltip）至 zh_cn/en_us；Tooltip 注解改单行 |
| 2026-08-03 | （本次） | 修复栏目内部未汉化：AutoConfig 顶层字段 option 键**不带分类前缀**（`option.<字段名>`；@ConfigEntry.Category 仅作 GUI 分组），修正全部 option/@Tooltip 键 |
| 2026-08-03 | （本次） | **休息维度 + 返回符咒**：数据包注册 monsterwaves:safe（flat 全空+代码空岛含功能方块/附魔台16书架）；玩家规则（不刷怪/不饥饿/免疫伤害/跳下传送）；返回符咒（合成/开局给予/冷却）；**维度开关** enabledDimensions；注意 1.20.1 用 MobSpawnEvent（无 LivingSpawnEvent） |
| 2026-08-03 | （本次） | 修复跳下空岛崩溃：遍历 `safeLevel.players()` 时直接 `handleFall` 传送修改列表触发 ConcurrentModificationException → 先收集后统一传送 |
| 2026-08-03 | （本次） | 休息维度景观：环境光 0→0.5（天空更亮）；空岛种 3 棵橡树 + 撒花/草 + 周围 3 个漂浮小岛；新增 `/monsterwaves safe reset` 重建空岛（已生成空岛需 reset 才能看到新景观） |
| 2026-08-03 | （本次） | 返回符咒贴图重绘：以 MC 床为原型（物品栏视角），手绘 16x16 斜置单层床（红床垫+白枕头+木床架） |
| 2026-08-03 | （本次） | 跳下空岛至主世界时传送至玩家重生点（床/出生点），新增配置开关 `fallToRespawnPoint`（默认 true） |
| 2026-08-03 | （本次） | 配置 UI 优化：合并 `fallToRespawnPoint`+`useCustomFallDestination` 两个布尔开关为单个枚举 `fallMode`（RESPAWN/CUSTOM 二选一，GUI 下拉切换） |
| 2026-08-03 | （本次） | 回滚枚举方案（下拉切换不好用）：改回布尔开关 `fallToRespawnPoint`，并通过 `GuiRegistry.registerPredicateTransformer` 实现**条件显示**（开启时隐藏自定义坐标 X/Y/Z 字段） |
| 2026-08-03 | （本次） | 配置 GUI **实时刷新**：客户端 tick 轮询开关值，变化时立即重建 Cloth Config 界面（不依赖保存退出）；注意 Cloth Config 11.1.136 无条目实时回调 API；修复静态初始化在 AutoConfig.register 前调用配置的崩溃隐患 |
| 2026-08-03 | （本次） | 返回符咒贴图按用户提供的参考图（`martirals/300px-Red_Bed_(N)_JE4.webp`，MC 红床 JE4 渲染）绘制：裁剪+放大+增强后输出 16x16 |
| 2026-08-03 | （本次） | 返回符咒贴图改为**完整参考图直接像素化**（resize 16x16，不裁剪/不放大/不增强） |
| 2026-08-03 | （本次） | **刷怪维度 arena**：数据包注册超平坦战场（grass/dirt/stone 三层）；原版自然生成拦截、本模组接管；`/monsterwaves battle` 传送；休息维度坠落目标默认改回 arena；配置 arena 段（enabled/spawnY） |
| 2026-08-03 | （本次） | 修复 arena 不刷怪：`MobSpawnEvent.PositionCheck` 无条件 DENY 把本模组 MOB_SUMMONED 生成也拦掉 → 加 `getSpawnType() == MobSpawnType.NATURAL` 条件，仅拦原版自然生成 |
| 2026-08-03 | （本次） | 生成引擎加诊断日志（怪物池空/位置失败/spawn null 三处 warn）；实测确认 arena 刷怪正常——**此前不刷怪根因为配置 json5 值异常（非代码 bug）**，用户修正配置后恢复 |
| 2026-08-03 | （本次） | **统一掉落系统**：`loot` 配置（normalLoot 条目：item/nbt/min-max/chance/isPlayerAffected），死亡掉落与属性球并行，概率=条目×难度×倍率、数量+每难度额外；**属性球清理**：`ball_cleanup` 配置（超时/上限/吸向玩家）；**战斗符咒**：`battle_charm`（右键传 arena，4 珍珠+1 铁剑合成，开局给予，冷却） |
| 2026-08-03 | （本次） | 属性球掉落加总开关 `ballDropEnabled`（统一掉落不受影响）；清理机制改造：**按配置物品名名单（cleanupItemNames）匹配**要清理的掉落物 + **追踪时间清理最早的一批**（超时消失/超上限按存在时长升序清理最早的） |
| 2026-08-03 | （本次） | 清理反馈与逻辑：**倒计时提醒**（<5 分钟提醒一次，之后每分钟一次，≥5 分钟不提醒）；**清理直接清完不排序**（超时清 + 超上限清超出数量）；**开始/完成聊天栏反馈**（完成含清理数量） |
| 2026-08-03 | （本次） | 修复提醒只触发一次：每次清理执行后重置 `lastRemindMin`，保证**每个清理周期**进入 <5 分钟都周期性提醒（默认 60 秒间隔因永远 <5 分钟曾导致只提醒一次） |
| 2026-08-03 | （本次） | 掉落列表汉化完善：`LootEntry` 加 `toString()`（列表项显示友好文本，替代对象地址）；补 normalLoot 提示与子字段键（item/NBT/数量/概率/抢夺） |
| 2026-08-03 | （本次） | 掉落列表项改为显示**本地化物品名**（中文环境显示"钻石"而非 minecraft:diamond）+ 数量/概率 |
| 2026-08-04 | （本次） | **tacz 属性不显示修复**：界面枚举改**白名单驱动**（遍历 attributeConfigs 键→resolveAttribute→player.getAttribute，不再依赖 getSyncableAttributes 过滤，保证自注册属性必然列出）+ syncable 兜底；日志确认挂载成功；加首次打开排查日志（`UI: tacz属性已注册=… 玩家有实例=…`） |
| 2026-08-04 | （本次） | 指令 Tab 补全修复：`skill reset <玩家> <属性>` 的属性参数加 `SUGGEST_ATTRIBUTES` 补全（白名单属性注册名，含 tacz）；其余参数（玩家/阶段/生物）已有补全 |
| 2026-08-04 | （本次） | 白名单默认扩充至 **10 项**（+幸运 luck、盔甲韧性 armor_toughness，用户反馈界面未显示；白名单外属性不显示属设计）；tacz 属性挂载加日志（启动日志可见 [MonsterWaves] tacz 测试属性已挂载）；tacz 组在界面列表**最底部**（按 namespace 排序）需滚动查看 |
| 2026-08-04 | （本次） | **战斗维度加厚+再抬高**：arena 地表 y=63→**y=159**（stone×158+dirt+grass，地基更厚防挖穿、更高配合光影），arenaSpawnY 64→160（代码+测试环境 json 同步） |
| 2026-08-04 | （本次） | **战斗维度光影下地面暗修复**：arena 超平坦地表原在 y=2，低于光影假定的"海平面"(y≈63)被当"地下"处理导致开启光影时地面变暗、高处正常；地表抬高至 y=63（stone×62+dirt+grass），arenaSpawnY 默认 4→64；已生成 arena 世界需删 `saves/<世界>/dimensions/monsterwaves/arena` 重新生成 |
| 2026-08-04 | （本次） | **FTB 版本线纠正（重大修正）**：FTB 版本前缀 1902=**1.19.2**、2001=**1.20.1**（此前误降 1902 线导致 ftb-quests 报 mc 1.19.2 范围错误）；编译依赖与 mods.toml 前置改为 **ftblibrary 2001.1.1-build.1**（maven 唯一可用 1.20.1 线，API 兼容验证通过）；环境精简为 ftblibrary 2001.1.1 单独（Quests/Teams/Ultimine 因 maven 2001 版本不配套【需 build.219+，只有 build.1】且 2001.2.13 崩溃【architectury EventFactory 越界】而备份移除）；⚠️ 若 2001.1.1 在环境仍崩（mixin 冲突），终极方案=mod 移除 FTB 依赖改内置 GUI |
| 2026-08-04 | （本次） | **修复测试环境 FTB Library 崩溃**：错误报告显示 ftb-library-forge-2001.2.13 加载时 architectury EventFactory 数组越界（`Index 1 out of bounds for length 0`），architectury 9.2.14 已是 1.20.1 最新无法升级；**将 FTB 全家桶降级到 1902 线**（Library 1902.4.2-build.700 / 任务 1902.5.10-build.497 / 团队 1902.2.14-build.165 / 连锁破坏 1902.4.2-build.136，来自 maven.ftb.dev/releases），旧 2001 jar 备份在测试MOD/mods_backup/FTB2001/；mods.toml 前置收紧 `[1902,2001)`；⚠️ FTB 任务 2001→1902 存档进度可能不兼容，启动前建议备份 saves |
| 2026-08-04 | （本次） | **加点 UI 重构**：折叠状态独立存 `Map<namespace,Boolean>` 字段（此前存在组对象内，rebuildGroups 每次重建导致折叠失效/条目异常），切换改延迟一 tick 重建；按钮构建抽为独立方法；战斗维度光照修复：arena dimension_type `ambient_light 0.0→0.5`（与 safe 一致，原无环境光导致偏暗） |
| 2026-08-04 | （本次） | 修复分组折叠无效：onClicked 中直接 refreshWidgets 会在事件分发中改列表导致失败，改为 `Minecraft.tell` 延迟到下一 tick 重建；大范围拾取新增**经验球**（pickupXp 配置，拉到玩家接触自动吸收） |
| 2026-08-04 | （本次） | 白名单外属性清理增强：① 每 5 秒周期兜底检查（PlayerTickEvent + cleanupOutOfWhitelist，配置移出白名单后自动清理返还、无需重进）；② `/monsterwaves reload` 重载 JSON 配置 + 全玩家重应用（解决手改配置 AutoConfig 缓存不刷新的问题） |
| 2026-08-04 | （本次） | 加点界面分组**可收起/展开**：组标题点击切换（▼展开/▶折叠，折叠显示属性数量）；折叠状态仅本次会话内存 |
| 2026-08-04 | （本次） | **全部属性默认为百分比加成**：AttributeConfig.percentage 默认改 true，默认白名单 8 项全部 percentage=true（每点 +10%）；提示：护甲 base 为 0，百分比乘 base 无效，若需护甲加点请改回数值（percentage:false） |
| 2026-08-04 | （本次） | 白名单每属性配置补全：`AttributeConfig` 新增 `percentagePerPoint`（每点百分比幅度，0=跟随全局）——所有白名单属性均可独立配置 enabled/percentage/maxPoints/percentagePerPoint；applyModifier 走 perAttributePercentagePerPoint |
| 2026-08-04 | （本次） | **v9.5.1 取消符咒冷却机制**：移除休息/战斗符咒的冷却检查与 NBT 冷却标记（`safeCooldown`、`battleCharmCooldown` 配置项及其 lang 一并删除），符咒改为纯消耗制（用一次没一个）；旧配置 json 中的冷却键被 Cloth 忽略，无影响 |
| 2026-08-04 | （本次） | **v9.5.0 改名与坠落传送对齐**：① `return_charm` 显示名「返回符咒」→「休息符咒」（注册名不变，存档/配方不受影响；lang 与源码提示全部更新）；② 空岛跳下自动传送（handleFall）目标为**战斗维度**时改用竞技场出生点 `(0.5, arenaSpawnY, 0.5)`（对齐 `teleportToArena`），不再用 `getSharedSpawnPos()`（可能偏离竞技场） |
| 2026-08-04 | （本次） | **v9.4.9 符咒完善**：① 回归符咒贴图**重新绘制**（PIL 生成 16x16 指南针图标，替代异常的 minecraft:item/compass 多帧贴图）；② 三符咒加入**原版创造物品栏「战斗用品 Combat」**（BuildCreativeModeTabContentsEvent，注意 1.20.1 用 getTabKey() 比较 ResourceKey）；③ 新增配置 `safe.giveHomeCharmOnJoin`（开局给予回归符咒，默认 true，休息维度栏目）+ lang；④ 开局发放逻辑加入回归符咒（ModEventHandler.onPlayerLoggedIn） |
| 2026-08-04 | （本次） | **v9.4.8 回归符咒 + 符咒配方统一**：① 新增 `home_charm`（回归符咒，右键回主世界重生点，非创造消耗，图标用**指南针** minecraft:item/compass，材料指南针）；② 三个符咒配方统一为**十字形**（pattern ` P /PXP/ P `，上下左右 4 末影珍珠 + 中间物品）：返回符咒=金苹果、回归符咒=指南针、战斗符咒=铁剑；③ `return_charm`/`battle_charm` 原 shapeless 配方改为 shaped |
| 2026-08-04 | （本次） | **v9.4.7 新增 `/monsterwaves leave`（全员）**：返回主世界——优先玩家重生点（床），否则主世界出生点；解决从安全/战斗维度无常规返回手段（SafeDimensionManager.teleportToSpawn 通用方法） |
| 2026-08-04 | （本次） | **v9.4.6 测试反馈修复**：① gunsmithlib `ammo_capacity`（弹匣容量）是**物品/枪械属性**（evaluateItemAttribute，不挂玩家）→ 玩家无实例、UI 不显示——已从默认白名单与显示名移除（玩家不可加；如需物品级加成由枪包/其他 mod 提供）；② 阶段 BUFF（mobEffects）**保留但默认空**（用户确认非破坏方案，BUFF 交由其他 mod 如 Apotheosis）；③ 测试手册同步更新（B-4/B-5/C-5 标注，B-5 请在 v9.4.4+ 重测 resetAll 残留修复） |
| 2026-08-04 | （本次） | **v9.4.5 新增 `/monsterwaves config save`**：Cloth Config GUI 修改只更新内存配置，**必须点界面「保存并退出(Save)」才写文件**（ESC 关闭不保存）；新增指令可将当前内存配置（含 GUI 已改未保存值）主动写入 config/monsterwaves.json |
| 2026-08-04 | （本次） | **v9.4.4 修复重置后数值不回落**：① `resetAll` 原只删 NBT 分配记录**未移除玩家 modifier**（属性加成残留，界面/服务端值保持旧值，需重新加点才"修正"）——已补：重置前遍历移除全部已分配属性的 modifier；② 加点界面当前值改为**实时读取客户端玩家属性**（rowTitle 每帧 getTitle 时 `player.getAttribute(attr).getValue()`，不再用构造快照），服务端 modifier 变更经属性同步即时反映 |
| 2026-08-04 | （本次） | **v9.4.3 指令审查**：全指令参数类型审查——唯一字符串参数（stage set id / skill reset attribute）均 greedyString 支持冒号；skill points 补无参数版本（默认当前玩家，与 stats 一致） |
| 2026-08-04 | （本次） | **v9.4.2 修复 skill reset 参数解析**：`skill reset <玩家> <属性>` 的属性参数原用 `StringArgumentType.string()`——Brigadier 无引号输入时冒号 `:` 不在 unquoted 字符集，`minecraft:generic.attack_damage` 报 "参数后应有空格分隔"（command.expected.separator）；改用 `greedyString()`（属性名可含冒号）；重置失败提示补充格式说明（reset 无需数量参数） |
| 2026-08-04 | （本次） | **v9.4.1 修复显示名与白名单语义**：① `displayName` 回退改 `Component.translatable(attr.getDescriptionId())`——删除 attributeDisplayNames 条目后不再显示原始注册名（generic.movement_speed），改用属性自身 lang（原版/任何 mod 均由其 lang 定义，如 attribute.name.generic.movement_speed → 移动速度）；② 明确语义：**是否显示/可加点由 attributeConfigs（白名单）决定**，attributeDisplayNames 只改显示名——删除 attributeConfigs 条目需重启或 `/monsterwaves reload`，applyAll 会清理白名单外分配并返还技能点 |
| 2026-08-04 | （本次） | **v9.4 gunsmithlib 枪械属性桥接（tacz 加点真正生效）**：参考《逆转未来2.2.0》整合包 BF Tree（bft）职业系统——其枪械加点用 `gunsmithlib:reload_speed`/`bullet_speed`（Gunsmith Library，mod.chloeprime.gunsmithlib，**TaCZ 实际读取的属性**，`MiscAttributeAdapter.rpm(LivingEntity)` 射击时读玩家属性），证明 tacz 不读 tacz namespace 占位属性。改造：① 删除 `ModAttributes`（tacz:gun_fire_rate/gun_reload_speed 占位全删）；② 白名单默认换成 gunsmithlib 5 项（rpm 射速/reload_speed 换弹/bullet_damage 子弹伤害/bullet_speed 子弹速度/ammo_capacity 弹匣容量，全部百分比型）；③ 测试环境安装 gunsmithlib-forge-1.20.1-tacz1.1.8-6.1.0.jar（依赖 tacz>=1.1.7，挂所有实体属性 EntityAttributeModificationEvent）；④ Map.of→Map.ofEntries（超 10 对）；⑤ 依赖为可选（未装 gunsmithlib 时白名单键解析不到→不显示，无碍） |
| 2026-08-04 | （本次） | 白名单外属性不受加点系统影响：applyAll 自动移除 modifier、清除分配并返还技能点；界面条目**固定顺序**（按 namespace 分组 + 注册名排序，不随已分配变化）；**按 mod 分类**（自动读取属性来源 mod，minecraft 置顶，组标题行+属性行） |
| 2026-08-04 | （本次） | **v9.3.1 修复**：① 重置后界面参数错乱——`S2CSyncData.from()` 的 values 原只携带已分配属性（allocated<=0 跳过），重置后 values 为空、界面退回本地旧值；改为携带**全部白名单属性**的服务端当前值，重置/清零后界面即时正确；② 配置界面/加点界面 tacz 属性行显示英文注册名——测试环境 json 缺 tacz 显示名，已补"射速(额外)/换弹速度(额外)"；③ 误提交清理（.reasonix/*.ps1 不入库）；已推送 GitHub main（57f52ca） |
| 2026-08-04 | （本次） | **v9.3 FTB Library 加点界面**：SkillScreen 重写为 `ButtonListBaseScreen`（自带搜索框/滚动/标题栏），列表首行重置按钮、属性行点击加点；依赖 ftb-library-forge 1902.4.2-build.700（实测版本，文档 1902.1.11-build.46 不存在；maven 路径 maven.ftb.dev/**releases**）
| 2026-08-04 | （本次） | **属性加点白名单**：`attributeConfigs`（Map<属性, AttributeConfig>）取代 attributeEnabled/percentageAttributes/perAttributeMaxPoints——未列出属性不可加点；默认仅 攻击/护甲/生命/速度(百分比)/挖掘速度/攻击速度；**每属性独立配置 enabled/percentage/maxPoints**；修复 CompoundTag.getCompound 对缺失键返回临时对象导致加点分配**从未持久化**的致命 bug（allocatedTag 辅助方法） |
| 2026-08-04 | （本次） | 修复配置界面汉化丢失：v9.0 重写 lang 时遗漏 general/spawn/difficulty 分类的 15 个 option 键（enabled/spawnInterval/minDistance/maxDistance/maxMobsPerPlayer/mobCountMin/mobCountMax/mobStatRadius/mobPool/难度增幅等）+ resetEnabled @Tooltip；补齐并全量校验（顶层字段 100% 覆盖；POJO 子字段仍受 Cloth 11.1.136 限制不显示翻译） |
| 2026-08-04 | （本次） | 修复加点界面交互：① 条目可点性不再依赖客户端缓存技能点（改由服务端权威校验，失败发红色提示），解决"技能点不足时全部条目点不动"；② S2C 同步包新增**服务端属性当前值**（values），界面即时显示新值（不等客户端属性同步包延迟），解决"加点后无反应"；③ 重置按钮防连点；④ skill gui 打开前请求同步 |
| 2026-08-04 | （本次） | 修复按键未集成游戏控制设置：`RegisterKeyMappingsEvent` 在 **mod 事件总线**（modBus）触发，此前注册到 Forge 总线导致回调从未执行（按键不出现、P 键无效）；改为 KeyBindings 注册 modBus、ClientEvents 保持 Forge 总线；补 `resetEnabled` @Tooltip 语言键 |
| 2026-08-04 | （本次） | **v9.0 技能点系统（重大重构，替代属性球）**：移除属性球全套（物品/掉落/拾取/清理/ball 指令/ball 配置/lang）；新增——获取模式 gainMode（LEVEL 默认升级得点/XP 积累经验/DISABLED）+ `SkillPointGainEvent` 自定义算法 API；动态枚举原版+模组属性（`getSyncableAttributes()`）；每点+1（百分比属性每点+10%）；P 键加点界面（SimpleChannel 同步，Button.builder）；skill 指令（points/add/set/reset/gui）；经验加成 drop.experience（LivingExperienceDropEvent）；旧属性球数据自动迁移（值/2→分配 + 总值20%补偿）；3 个 API 事件（Gain/Spent/Reset）；1.20.1 注意：AttributeMap 无 getAttributes/getAllAttributes、Button 6 参构造器不存在 |
| 2026-08-03 | （本次） | **阶段配置化**：硬编码 STAGES 改为 Cloth Config 可配置（`stage.stages` 列表，各阶段 id/difficulty/duration 分别调整）；难度作为整体参数参与各类运算（怪物属性/掉落概率/数量） |
| 2026-08-03 | （本次） | **阶段配置扩展**：每阶段新增 `mobListOverride`（专属怪物池）、`attributeMultipliers`（生命/攻击/护甲倍率）、`mobEffects`（自带BUFF，支持模组效果）；怪物属性新增护甲加成（armorBonusPerLevel）；生成引擎应用阶段池/倍率/效果 |
| 2026-08-03 | （本次） | **属性映射配置化**：新增 `attributeMapping`（类型名→原版/模组属性注册名），PlayerDataManager 动态解析任意属性（兼容其他 mod 自定义属性）；历史 3 类型保留固定 UUID 兼容旧存档 |
| 2026-08-03 | （本次） | **通用属性球** `attribute_ball`：物品统一、NBT（`mw_type`）存实际类型名——自定义属性球类型无需新增物品即可生效；掉落/拾取/清理全部适配 |
| 2026-08-03 | （本次） | 修复自定义属性球类型：① `attributeMapping` 无映射时 **fallback 将类型名当属性注册名**（如 `ballTypes` 直接写 `minecraft:generic.movement_speed` 也可用）；② **速度类属性用比例加成**（每点 +1% 基础速度，MULTIPLY_BASE），避免 ADDITION 导致速度爆炸 |
| 2026-08-03 | （本次） | ① **属性球掉落权重** `attributeWeights`（类型名→权重，0=不掉）；② **指令 type 参数支持冒号**（新 AttributeTypeArgument，`ball give 玩家 minecraft:generic.movement_speed 10` 可输入）；③ **默认 3 类型名改用 MC 属性注册名**（旧名 ATTACK 等仍兼容：mapping 保留、旧球/旧配置可用）；④ 显示名用原版属性本地化名（中文） |
| 2026-08-03 | （本次） | 掉落列表子字段英文问题：Cloth Config 对 List<POJO> 子字段翻译键不生效（无公开文档），`normalLoot` 改为 **List\<String\> JSON 条目**（列表标题汉化、项为数据文本，无英文子字段名）；dropLoot 用 Gson 解析 |
