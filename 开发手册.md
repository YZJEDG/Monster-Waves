# 怪物狂潮（Monster Waves）— 开发手册

> 调试向手册，与《mod概述.md》（规格 v8.3）配套。**每次完成模块后更新本文件**。
> 当前版本对应：**MVP 阶段完成**（生成引擎 / 属性球 / 玩家属性 / 阶段 / 基础指令），Forge 47.4.10。

---

## 1. 构建与运行

| 项目 | 内容 |
|------|------|
| 构建命令 | `gradlew.bat build`（项目根目录，Windows） |
| 产物 | `build/libs/monsterwaves-0.1.0.jar` |
| 安装位置 | `D:\game\mc\.minecraft\versions\TAC_Z 神化\mods` |
| JDK | 本地 `.jdk\jdk-17.0.19+10`（gradlew.bat 自动探测，无需配 JAVA_HOME） |
| Gradle | wrapper 8.4，用户缓存 `.gradle_home`（gradlew.bat 自动探测 GRADLE_USER_HOME） |
| 镜像 | 腾讯（gradle 分发）/ 阿里云（maven 依赖）/ maven.minecraftforge.net / parchmentmc |
| Forge | `1.20.1-47.4.10`，mappings `parchment 2023.09.03-1.20.1` |

**构建排错**：控制台 GBK 乱码时用 `iconv -f GBK -t UTF-8 日志文件` 转码；首次构建慢（下载依赖）。

## 2. 源码结构（src/main/java/com/mcmod/monsterwaves/）

| 类 | 职责 |
|----|------|
| `MonsterWavesMod` | @Mod 主类。**必须无参构造器**（1.20.1 限制）；用 `FMLJavaModLoadingContext.get().getModEventBus()` 注册物品（47.4.10 已 deprecated，勿改构造器注入——那是 1.20.4+ 机制） |
| `config/MWConfig` | 全部硬编码配置常量（后续阶段迁往 Cloth Config） |
| `data/PlayerDataManager` | 玩家属性 NBT 存储 + AttributeModifier 应用（固定 UUID，ADDITION） |
| `stage/StageManager` | 阶段列表 + 切换广播 |
| `stage/StageData` | 阶段状态 SavedData（持久化到主世界 level.dat，键 `monsterwaves_stage`） |
| `spawn/MobSpawnManager` | 生成引擎（tick 调度、位置选择、生物池、难度应用） |
| `item/ModItems` | 物品注册（DeferredRegister，3 种属性球） |
| `item/AttributeBallItem` | 属性球物品（携带属性类型） |
| `event/ModEventHandler` | 服务端事件：tick（阶段+生成+球吸附）、登录/重生/换维度属性应用、击杀掉落、指令注册 |
| `command/MonsterWavesCommand` | /monsterwaves 指令树 |

资源：`src/main/resources/`：`pack.mcmeta`（**必须存在**，缺失则 Forge 不索引 assets）、`META-INF/mods.toml`、`assets/monsterwaves/{lang,models,textures}`。

## 3. 硬编码配置（MWConfig，与规格路径对应）

| 常量 | 值 | 说明 |
|------|-----|------|
| `SPAWN_INTERVAL` | 40 | 生成检测间隔（tick） |
| `MIN_DISTANCE` / `MAX_DISTANCE` | 20 / 32 | 生成距玩家距离范围 |
| `MAX_MOBS_PER_PLAYER` | 30 | 玩家 48 格内本mod怪物上限 |
| `MOB_COUNT_MIN` / `MOB_COUNT_MAX` | 1 / 2 | 每波数量范围 |
| `MOB_POOL` | zombie:5, skeleton:3, creeper:2 | 生物池（注册名→权重） |
| `HEALTH_BONUS_PER_LEVEL` | 0.2 | 每点难度生命增幅（乘） |
| `ATTACK_BONUS_PER_LEVEL` | 0.5 | 每点难度攻击增幅（加） |
| `BALL_BASE_CHANCE` | 0.2 | 普通怪属性球基础掉落率 |
| `BALL_VALUE` | 1 | 每个球加成点数 |
| `BALL_TYPES` | ATTACK/HEALTH/ARMOR | 属性类型 |

## 4. 阶段（StageManager.STAGES，硬编码）

| 索引 | id | difficulty | duration(tick) |
|------|----|-----------|----------------|
| 0 | 🌱 萌芽期 | 1.0 | 6000 |
| 1 | ⚔️ 激战期 | 2.5 | 12000 |
| 2 | 🔥 终局之战 | 5.0 | -1（无限） |

- 自动推进：计时到 duration 切换，广播全体玩家；手动：`/monsterwaves stage next|prev|set <id>`
- 持久化：SavedData `monsterwaves_stage`（stageIndex + stageTimer），跨重启保留

## 5. 指令（/monsterwaves，全部支持 Tab 补全）

| 指令 | 权限 | 说明 |
|------|------|------|
| `spawn <生物id> [数量]` | op | 生成带标记+难度应用的怪；非 Mob 实体自动移除（防强转崩溃）。`<生物id>` 补全所有已注册非 MISC 实体（如 `minecraft:zombie`） |
| `stats [玩家]` | 全员 | 查看属性累计。`[玩家]` 补全在线玩家 |
| `difficulty` | 全员 | 当前阶段+难度 |
| `stage info / next / prev / set <id>` | info 全员，其余 op | 阶段管理。`set <id>` 补全阶段 id（**greedyString**：id 含空格如"🌱 萌芽期"也能输入） |
| `ball give <玩家> <类型> <数量>` | op | 直接加属性（测试用）。`<类型>` 补全 ATTACK/HEALTH/ARMOR |
| `ball set <玩家> <类型> <数值>` | op | **直接设置**属性值为指定数值（覆盖，0~99999） |
| `player add/set <玩家> <类型> <数值>` | op | 与 ball give/set 等价（对齐规格 player 指令组） |
| `player reset <玩家>` | op | 重置该玩家全部属性为 0 |
| `player list` | op | 列出所有在线玩家 |

## 6. 数据与标记

- 玩家 NBT：`monsterwaves_data` → `attributes` → `{ATTACK, HEALTH, ARMOR}`（整数）
- 玩家属性映射：`ATTACK→generic.attack_damage`、`HEALTH→generic.max_health`、`ARMOR→generic.armor`，UUID 固定（见 PlayerDataManager），ADDITION 直加
- 怪物标记：`monsterwaves_spawned=true`（生成引擎打标，用于数量统计 + 掉落判定）
- 属性球标记：`monsterwaves_ball_claimed`（防同 tick 多玩家重复拾取）

## 7. 属性球机制（经验球式）

- 掉落：击杀带标记怪，概率 = `min(1, 0.2 × 难度)`，随机类型，`setNeverPickUp()` + `setNoGravity(true)`（关闭 ItemEntity 自带重力，重力由服务端模拟）
- 移动（对齐原版 ExperienceOrb.tick）：服务端每 tick 处理玩家 3 格内球——
  - 出生延迟：`tickCount < 10`（0.5 秒）只受重力自由下落，不被吸引
  - 重力 `BALL_GRAVITY=0.04` + 空气阻力（速度乘 0.98），防止上飘
  - 吸引：位移归一化到 3 格，`d12=1-归一化距离`，加速度 `d12²×0.1` 向玩家**胸口**（eyeHeight/2）——越近越快，非直接设速
  - `hasImpulse=true` 强制同步速度到客户端，保证渲染跟随
- 吸收：中心距离 ≤1 格 → 应用属性 + 提示 + 音效，永不进背包
- 原因：**Forge 1.20.1 无 `ItemEntityPickupEvent`（1.20.5+ 才有）**，`PlayerEvent.ItemPickupEvent` 是拾取后且不可取消 → 采用 tick 接触检测

> 常量：`BALL_ATTRACT_RANGE=3.0`、`BALL_PICKUP_DISTANCE_SQ=1.0`、`BALL_GRAVITY=0.04`、`BALL_ATTRACT_DELAY=10`、`BALL_FLY_SPEED`（已删除，改为衰减加速）

## 8. 已知限制 / 调试要点

| 项 | 说明 |
|----|------|
| 生成维度 | MVP 在所有维度生成（未做维度覆盖/刷怪维度，第二阶段实现） |
| 精英/Boss | 未实现（MVP 仅普通怪） |
| 配置 | 全硬编码（MWConfig），第二阶段迁 Cloth Config（规格前置，整合包已有 cloth-config-11.1.136） |
| 控制台乱码 | gradle 输出 GBK，用 iconv 转码 |
| 模型/资源 | **pack.mcmeta 必须随 jar 打包**（缺失 → Forge 不索引 assets → 模型/语言全失效） |
| @Mod 构造器 | 1.20.1 **只支持无参构造器**（改 IEventBus 注入会 NoSuchMethodException 崩溃） |
| FMLJavaModLoadingContext | 47.4.10 deprecated，但 1.20.1 无替代，用 @SuppressWarnings 压制 |
| 游戏内验证 | 进主世界看刷怪 → 击杀看掉落 → 靠近看吸附/提示；`/monsterwaves difficulty|stage info|stats` 查状态 |

## 9. 后续阶段规划（对应开发建议.md）

1. **第二阶段**：Cloth Config 全参数可配、休息维度+返回符咒、统一掉落系统、完整指令集（最大难点：`LevelStem`+`DimensionType` 自定义维度注册）
2. **第三阶段**：特定怪物掉落、属性权重/上限、大范围拾取、战斗符咒、苦痛传递附魔（TaCZ 兼容）、怪物自带 BUFF、属性球清理、HUD、JEI/EMI

## 10. 变更记录

| 日期 | 提交 | 内容 |
|------|------|------|
| 2026-08-03 | 49e1189 | MVP 基线：核心循环全套 |
| 2026-08-03 | c2d76ac | 审查修复 7 项（强转/位置偏移/持久化/重复拾取等） |
| 2026-08-03 | d464de0 | 属性球经验球式（悬浮+吸附+吸收） |
| 2026-08-03 | a6d268c | 补 pack.mcmeta；Forge→47.4.10；主类构造器注入（**随后撤回**） |
| 2026-08-03 | aa949eb | 恢复无参构造器（修复 NoSuchMethodException 启动崩溃） |
| 2026-08-03 | （本次） | 指令 Tab 补全（spawn 实体 / stage set id / ball 类型）；stage set 改 greedyString 支持含空格阶段 id |
| 2026-08-03 | （本次） | 新增独立《指令手册.md》（格式 `<指令>:<作用> 参数解析`） |
| 2026-08-03 | （本次） | 指令新增 set 功能：`ball set`（覆盖设置属性值）、`player` 组（add/set/reset/list，对齐规格 15.7） |
| 2026-08-03 | （本次） | 修复属性球往天上飞：按经验球机制重写移动（重力 0.04 + 空气阻力 + 衰减加速 + 出生延迟 + 目标胸口高度 + hasImpulse 同步） |
