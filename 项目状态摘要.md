# 怪物狂潮（Monster Waves）— 项目状态摘要（恢复用）

> 本文档是浓缩版状态卡，配合《开发手册.md》《指令手册.md》使用。对话记录压缩后，凭本文档可快速恢复开发上下文。

## 项目定位

Minecraft 1.20.1 Forge 模组 `monsterwaves`（怪物狂潮）：持续刷怪 + 击杀掉属性球永久强化玩家。完整规格见 `mod概述.md`（v8.3）。

## 构建与交付（约定）

- 构建：`gradlew.bat build`（项目根目录；本地 JDK17 `.jdk`、gradle 缓存 `.gradle_home` 自动探测）
- 部署：`bash deploy.sh` → 自动备份旧版到 `测试MOD\mods_backup`、复制新 jar 到 `D:\game\mc\.minecraft\versions\测试MOD\mods`
- 每轮交付：更新《开发手册.md》变更记录（含 git hash）→ 构建 → deploy → 提交
- 调试：用户实测 + 错误报告 zip（放项目根目录）；日志在 `测试MOD\logs\latest.log`
- 测试环境：Forge 47.4.20，mods 含神化/FTB/TaCZ 等（环境崩溃多为第三方冲突，与 mod 无关）

## 技术栈

Forge 47.4.10（构建目标，兼容 47.4.20 运行）、parchment 映射、Cloth Config 11.1.136（AutoConfig，`config/monsterwaves.json5`）、Java 17。

## 已完成模块（全部构建通过、用户实测确认）

| 模块 | 要点 |
|------|------|
| **v9.0 技能点系统** | **替代属性球**：升级得点（默认）/XP/DISABLED 三模式 + SkillPointGainEvent 自定义算法；动态枚举原版+模组属性（getSyncableAttributes）；每点+1（百分比属性+10%）；P 键加点 GUI（SimpleChannel 同步）；skill 指令；经验加成；旧属性球数据自动迁移（值/2+20%） |
| Cloth Config 配置化 | 全部参数 GUI 可调（中文化、实时刷新、条件显示）；Mods 列表→Config 按钮（注册 ConfigScreenFactory 扩展点兼容 Catalogue） |
| 休息维度 safe | 数据包维度 + 代码空岛（功能方块/附魔台16书架/树花浮岛景观）、不刷怪/不饥饿/不死/跳下传送、`safe reset` 重建 |
| 返回符咒 | 合成（4珍珠+1金苹果）、开局给予、冷却、贴图为 MC 床渲染图像素化 |
| 刷怪维度 arena | 数据包超平坦战场、原版生成拦截（MobSpawnEvent.PositionCheck 仅 NATURAL）、`battle` 指令 |
| 战斗符咒 | 合成（4珍珠+1铁剑）、右键传 arena、冷却 |
| 统一掉落 | `normalLoot`（POJO 条目）、概率=条目×难度×倍率、数量+每难度额外、与属性球并行 |
| 属性球清理 | 名字名单匹配、超时/上限清理、周期提醒（<5min 每周期）+ 开始/完成反馈 |
| 大范围拾取 | 半径/物品/属性球/黑名单/onlyOwnDrops（掉落物归属标记 `monsterwaves_owner`） |
| 阶段配置化 | `stage.stages` 列表：id/difficulty/duration/mobListOverride/attributeMultipliers/mobEffects（阶段 BUFF） |
| 生成引擎/阶段/统一掉落/大范围拾取/休息与刷怪维度/符咒 | 保留（v8 功能，见开发手册第 2-5 节） |

## 当前进行中 / 待办

- 待用户实测 v9.0：升级得点 → P 键加点 → 属性生效；旧存档属性球数据自动迁移
- 待后续：v9.1 属性按模组分组织、v9.2 仇恨系统/无限堆叠/开关统一、v9.3 FTB GUI、精英/Boss 体系（eliteChance/bossChance + eliteLoot/bossLoot + 属性倍率）、阶段级精英/Boss 概率覆盖、完整指令集、HUD、JEI/EMI

## 关键技术坑（1.20.1，已踩过）

1. `@Mod` 类**只支持无参构造器**（IEventBus 注入是 1.20.4+；用 `FMLJavaModLoadingContext.get().getModEventBus()`，@SuppressWarnings("deprecation")）
2. **`pack.mcmeta` 必须**（缺失则 Forge 不索引 assets，模型/语言全失效）
3. 无 `ItemEntityPickupEvent`（1.20.5+）→ 属性球用服务端 tick 接触检测
4. 无 `LivingSpawnEvent` → 用 `MobSpawnEvent.PositionCheck`（且要判断 `spawnType==NATURAL`，否则拦掉本 mod 生成）
5. `LevelAccessor` 无 `dimension()`（instanceof Level）
6. `StringArgumentType.word()/string()` **不允许冒号** → spawn 用 `ResourceLocationArgument.getId()`，属性类型用自定义 `AttributeTypeArgument`
7. Cloth Config 11.1.136：`@Config` 无 `type` 属性（规格 ConfigType.SERVER 写法不存在）；`List<POJO>` 子字段翻译键不生效（英文显示，用开发手册字段表兜底）；`Map` GUI 显示有限（json5 手编）；无条目实时回调（客户端 tick 轮询重建）
8. `BlockPos.MutableBlockPos.below()` 原地修改（用 immutable 副本）
9. 遍历 `level.players()` 时传送会 CME（先收集后统一传送）

## 记忆（已存 project scope）

- 交付流程：手册+部署+备份（`bash deploy.sh`）
- 每次完成后导出开发手册
- 开发路线：Cloth Config 前置，对接其他 mod API（FTB/TaCZ）最后做

## 恢复要点（对话压缩后从此继续）

1. 读本摘要 + 《开发手册.md》第 1/3/4 节（构建/配置/阶段）
2. git log 看最新提交（当前主线：属性球权重/通用球）
3. 用户测试反馈驱动迭代；错误报告 zip → 解压到 `.errreport` 分析
