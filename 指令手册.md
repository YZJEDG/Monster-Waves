# 怪物狂潮（Monster Waves）— 指令手册

> 格式：`<指令>:<作用> 参数解析`
> 适用版本：MVP（Forge 1.20.1 / monsterwaves-0.1.0）
> 所有指令以 `/monsterwaves` 为根（全称，无别名），支持 Tab 补全；权限为 Forge 原版 op 等级（`hasPermission(2)`，第二阶段将迁移至规格的 `mw.*` 权限节点）。

## 配置与状态

- `/monsterwaves stats [玩家名]:查看玩家属性累计值。 参数解析：[玩家名] 在线玩家（Tab 补全），缺省为执行者自己；显示 ATTACK/HEALTH/ARMOR 三项累计加成`

- `/monsterwaves difficulty:显示当前阶段与难度系数。 参数解析：无参数；输出阶段 id 与难度乘数（如 x2.5）`

- `/monsterwaves safe:传送至休息维度（管理员）。 参数解析：无参数；无冷却直接传送，空岛含工作台/熔炉/箱子/附魔台等设施`

- `/monsterwaves safe reset:重置并重建空岛。 参数解析：无参数；覆盖重建空岛（含树/花/漂浮小岛等景观），用于已生成空岛刷新`

- `/monsterwaves battle:传送至刷怪维度（管理员）。 参数解析：无参数；超平坦战场，本模组怪物在此生成`

## 阶段管理

- `/monsterwaves stage info:显示当前阶段详情。 参数解析：无参数；输出阶段索引/名称/难度系数/剩余时间（秒）或"无限"`

- `/monsterwaves stage next:切换至下一阶段（自动广播）。 参数解析：无参数；到列表末尾后循环回第一个`

- `/monsterwaves stage prev:切换至上一阶段（自动广播）。 参数解析：无参数；到列表开头后循环至末尾`

- `/monsterwaves stage set <阶段id>:切换至指定阶段（自动广播）。 参数解析：<阶段id> 阶段名称（Tab 补全，纯文字无符号，如 `萌芽期` / `激战期` / `终局之战`）；未找到时报可用阶段列表`

## 生成

- `/monsterwaves spawn <生物id> [数量]:在玩家身边生成指定怪物。 参数解析：<生物id> 实体注册名（Tab 补全所有非 MISC 实体，如 `minecraft:zombie`；非 Mob 实体自动移除）；[数量] 1~100，缺省 1；生成物带 `monsterwaves_spawned` 标记并按当前难度调整生命/攻击`

## 技能点（v9.0，替代原属性球）

- `/monsterwaves skill [玩家]:查看技能点与已分配属性。 参数解析：无参=自己；可选 <玩家> 在线玩家；显示可用技能点、总已分配、每属性分配`

- `/monsterwaves skill add <玩家> <数量>:为玩家发放技能点（管理员）。 参数解析：<玩家> 在线玩家；<数量> 1~99999`

- `/monsterwaves skill set <玩家> <数量>:设置玩家技能点（管理员）。 参数解析：<玩家> 在线玩家；<数量> 0~99999`

- `/monsterwaves skill reset <玩家> [属性]:重置加点（管理员）。 参数解析：<玩家> 在线玩家；[属性] 可选属性注册名（如 `minecraft:generic.attack_damage`）；无属性=重置全部（免费返还），有属性=仅重置该属性（返还该属性点数）`

- `/monsterwaves skill gui:打开技能加点界面。 参数解析：无参数；发送 S2C 包在客户端打开（等价按 P 键）`

## 玩家数据管理

- `/monsterwaves player list:列出所有在线玩家。 参数解析：无参数；显示在线玩家名单与数量`

## 权限一览

| 指令 | 权限 |
|------|------|
| stats / difficulty / stage info / skill / skill gui | 全员 |
| spawn / stage next / stage prev / stage set / skill add / skill set / skill reset | op（等级 2） |
